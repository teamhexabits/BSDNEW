# PrivateFrame complete configuration reference

This bundled reference is available offline after installation. It covers every current public YAML/CLI override, including advanced settings omitted from the curated `describe` response. Advanced settings remain supported; omission from discovery does not remove their behavior.

Public override paths: **279**. Configuration schema version: **1**.

<!-- configuration-contract-sha256: 42333431872981999dd9f28e2addaab5ff7065f00a3f669efda9fb2a644f9893 -->

Generated by `python -m insightface.app.privateframe.config_reference`; do not hand-edit. Check for drift with `python -m insightface.app.privateframe.config_reference --check`. Use `--output PATH` to write a separate copy when the installed package is read-only.

## Loading and overriding configuration

Analysis starts from bundled `configs/base.yaml`. A custom `--config PATH` is a YAML mapping with `schema_version: 1`; its fields overlay the bundled Base unless `base_config` selects another base YAML file. `base_config` is a nonempty path, resolved relative to the overlay file when relative. It selects the immediate base document; it is not a recursive chain of overlays. `schema_version` and `base_config` are document fields, not dotted CLI options.

Precedence: selected base YAML → custom YAML overlay → CLI dotted overrides. For rendering, the result JSON's `render_defaults` are overlaid by `--render-config` and then CLI `render.*` overrides. Render accepts only render settings; analyze/process and doctor accept the public configuration fields described below.

Mappings merge recursively. Arrays supplied in YAML replace the entire array rather than merging elements. A CLI array override also replaces the array; indexed CLI overrides modify an existing element and cannot append past its length. Supply an array in custom YAML before using indexed CLI overrides, or pass one whole-array CLI override. Duplicate paths and overlapping parent/child CLI paths are rejected, so replacing an array and overriding one of its indices in the same command is invalid. The `rate_control` mapping is replaced as a unit only when the whole mapping is supplied; individual leaf overrides retain other sibling values.

Dotted syntax is `--section.field VALUE` or `--section.field=VALUE`; values use YAML scalar/array syntax. Quote arrays and strings as required by your shell. Boolean values are `true`/`false`; `null` is accepted only where documented. Unknown keys and invalid types are rejected. `command --dry-run` is authoritative for cross-field and encoder-specific checks.

```yaml
schema_version: 1
scan:
  max_analysis_fps: 15
render:
  redaction:
    method: mosaic
```

```sh
insightface-privateframe process --input video.mp4 --output-dir output --config overlay.yaml --dry-run
insightface-privateframe process --input video.mp4 --output-dir output --recognition.mode exempt --recognition.reference_dir reference_photos --dry-run
insightface-privateframe process --input video.mp4 --output-dir output --recognition.mode blur_only --recognition.reference_dir reference_photos --dry-run
```

A table default is the literal packaged Base value, not an environment-resolved path or a measured runtime value. **Not set (optional)** differs from an explicit `null`: the field is absent from Base and its contextual behavior is explained in the description. `yaml` means no static leaf type is declared by the generated contract; the accompanying semantic requirements and dry-run still apply. Numeric ratios are dimensionless unless a different unit is stated. IoU is intersection over union; p50 is a median.

## Optional arrays and cross-field rules

`revalidation.passes` may be omitted or `null` to use shared revalidation settings. Otherwise it is a nonempty array of mappings with `name`, `input_size`, and `crop_expansion`. Names become nonempty unique text; input_size is a positive integer multiple of 32, and crop_expansion is a positive finite number. Indexed paths such as `revalidation.passes.0.input_size` work when the selected/overlay YAML already supplies those entries. To create the array from the CLI, supply a whole-array override without any indexed descendant override in that command. These paths are not listed as current Base indices because Base has no such array entries.

`scan.passes` entries may contain optional `candidate_filter` settings. An enabled filter requires area fraction in [0,1], positive minimum height/width ratio, and an optional maximum ratio no smaller than that minimum. The aspect-ratio exemption area fraction must lie between the minimum area fraction and 1.

For `render.video_output.rate_control`, crf/cq require exactly mode and quality; vbr requires mode and bitrate and optionally max_bitrate; cbr requires mode and bitrate and optionally buffer_size. Bitrate values are positive integers or strings with decimal k/m/g multipliers (1000/1000000/1000000000), bounded by signed 64-bit integer capacity after parsing. Changing only the mode leaf to vbr and adding bitrate leaves Base's quality value in place and is invalid. Replace the whole mapping when switching modes, as below. A quality-only leaf override is valid while staying in crf/cq.

```sh
insightface-privateframe process --input video.mp4 --output-dir output --render.video_output.rate_control '{mode: vbr, bitrate: 4M}' --dry-run
insightface-privateframe process --input video.mp4 --output-dir output --render.video_output.rate_control.quality 22 --dry-run
```

Enabling feathering requires explicit `render.redaction.feather.ratio` in [0,0.5] and positive `min_pixels`. These fields have no Base default. Shrinking box_scale or blending redaction edges reduces covered/opaque pixels; inspect representative results when changing coverage.


## output

Retention controls affect diagnostic files, not the intended privacy policy. Audit/debug evidence is for investigation and increases artifact I/O.

| Override path | Type | Base default | Meaning and constraints |
|---|---|---|---|
| `output.artifacts_level` | string | `"final"` | Retain only the reusable result in final mode; audit also writes development evidence and summaries, while debug retains additional intermediate records. Values: `"final"`, `"audit"`, `"debug"`. |

## models

Package task files, hashes, normalization, and resolved model paths come from the manifest; they are not user override fields. The selected model root is authoritative.

| Override path | Type | Base default | Meaning and constraints |
|---|---|---|---|
| `models.name` | string | `"raccoon_s"` | Select the manifest-backed face detector, verifier, and optional recognizer package. Values: `"raccoon_s"`, `"raccoon_l"`. |
| `models.root` | string | `"~/.insightface"` | Local InsightFace root containing models/&lt;package&gt;; this selected root is authoritative. |
| `models.detection.nms_iou_threshold` | number | `0.4` | IoU threshold used to suppress overlapping detections within the detector. Minimum: 0.0; Maximum: 1.0. |
| `models.detection.max_detections` | integer | `300` | Maximum detections retained by each detector invocation. Minimum: 1. |

## runtime

Provider and codec availability depend on the installation. Use doctor and the selected command's --dry-run before execution; these checks do not download models or create inference sessions.

| Override path | Type | Base default | Meaning and constraints |
|---|---|---|---|
| `runtime.provider` | string | `"auto"` | Inference execution provider; auto follows the available CoreML, CUDA, then CPU preference. |
| `runtime.scrfd_static_shape_sessions` | boolean | `true` | Reuse a separate fixed-input-shape SCRFD session for each detector resolution; false selects the dynamic-session compatibility path. |
| `runtime.intra_op_threads` | integer | `1` | ONNX Runtime thread count within an operator. |
| `runtime.inter_op_threads` | integer | `1` | ONNX Runtime thread count across operators. |

## recognition

Choose all to blur everyone, blur_only to blur people in reference photos, or exempt to keep people in those photos visible. The two photo modes require reference_dir and use JPG/JPEG/PNG/WebP images directly inside that folder (case-insensitive extensions); person names and subfolders are unnecessary. Hidden files and symlinks are skipped, and byte-identical photo copies are deduplicated. Different photos of the same person remain valid references. Only the largest detected face in each photo is considered, with no fallback to smaller faces. Multi-face selection, skipped-photo reasons, and import totals are logged on stderr; no usable reference faces is an error. With unknown_action=auto, uncertain or unmatched people remain visible in blur_only and are blurred in exempt. Model loading or inference errors stop processing. A higher similarity threshold is not the only recognition gate.

| Override path | Type | Base default | Meaning and constraints |
|---|---|---|---|
| `recognition.mode` | string | `"all"` | all blurs every detected face without recognition; blur_only blurs people matched to reference photos; exempt keeps matched people visible. With unknown_action=auto, unmatched or uncertain people remain visible in blur_only and are blurred in exempt. Values: `"all"`, `"blur_only"`, `"exempt"`. |
| `recognition.reference_dir` | string or null | `null` | Local folder of JPG, JPEG, PNG, or WebP reference photos, with no person subfolders or names. Each photo contributes only its largest detected face; an unusable largest face causes the photo to be skipped. Multiple people and multiple photos per person may share one folder. Both selective modes require at least one usable reference face. all does not read this folder. Explicit null accepted. |
| `recognition.unknown_action` | string | `"auto"` | Action for unmatched or uncertain people: auto resolves to keep in blur_only and blur in exempt; blur or keep explicitly overrides that action. all always blurs every detected face. keep can leave an unrecognized target visible. The effective policy is stored in analysis JSON and reused during rendering; inference errors fail the task. Values: `"auto"`, `"blur"`, `"keep"`. |
| `recognition.profile` | string | `"balanced"` | Set the default cap of 1, 3, or 5 eligible temporal identity samples per track for fast, balanced, or accurate; max_frames_per_track can override this cap. Values: `"fast"`, `"balanced"`, `"accurate"`. |
| `recognition.similarity_threshold` | number | `0.4` | Base cosine similarity gate for identity confirmation; temporal-evidence offsets and other recognition gates still apply. Minimum: 0.0; Maximum: 1.0; Unit: cosine similarity. |
| `recognition.max_frames_per_track` | integer or null | Not set (optional) | Optional explicit limit of 1–32 identity samples per track; absent or null uses the recognition profile. Minimum: 1; Maximum: 32; Explicit null accepted. |

## scan

Angles are degrees. Input sizes are positive multiples of 32. Detector-view arrays must be nonempty with unique names. Candidate-filter bounds apply when enabled. Scene-cut flow distances use the downscaled grayscale signature, not source-resolution pixels.

| Override path | Type | Base default | Meaning and constraints |
|---|---|---|---|
| `scan.max_analysis_fps` | number | `15` | Default Fast mode targets 15 regular detector samples per second of input video; this is a soft ceiling, integer stride uses 5% tolerance, and forced scans may exceed it. Use 30 for greater temporal coverage. This is not a wall-clock throughput target, and output FPS is unchanged. Must exceed: 0.0; Unit: frames per second of input video. |
| `scan.session_sharing` | string | `"single_session_parallel"` | Permit concurrent or serialized calls to the shared full-frame detector session. Values: `"single_session_parallel"`, `"single_session_serial"`. |
| `scan.workers` | integer | `18` | Number of concurrent full-frame scanning workers. |
| `scan.pipeline_depth` | integer | `6` | Number of video frames allowed in the scan pipeline queue. |
| `scan.passes` | array | `[{"angles":[0,90,-90],"confidence_threshold":0.5,"horizontal_padding_ratio":0.1,"input_size":800,"name":"scrfd10g_pad10_800_cardinal","vertical_padding_ratio":0.1},{"angles":[0],"candidate_filter":{"aspect_ratio_exempt_min_area_fraction":0.5,"enabled":true,"min_box_area_fraction":0.01,"min_height_width_ratio":0.8},"confidence_threshold":0.5,"horizontal_padding_ratio":0.2,"input_size":256,"name":"scrfd10g_pad20_256_upright_large_face","vertical_padding_ratio":0.2}]` | Ordered array of full-frame detector views; each view declares its own angles, input size, padding, and confidence gate. |
| `scan.passes.0.name` | string | `"scrfd10g_pad10_800_cardinal"` | Unique name identifying this detector view in diagnostics. |
| `scan.passes.0.angles` | array | `[0,90,-90]` | Nonempty array of detector-view rotations in degrees; supported values are 0, 90, and -90. |
| `scan.passes.0.angles.0` | integer | `0` | Rotation in degrees for this indexed detector-view entry; supported values are 0, 90, and -90. |
| `scan.passes.0.angles.1` | integer | `90` | Rotation in degrees for this indexed detector-view entry; supported values are 0, 90, and -90. |
| `scan.passes.0.angles.2` | integer | `-90` | Rotation in degrees for this indexed detector-view entry; supported values are 0, 90, and -90. |
| `scan.passes.0.input_size` | integer | `800` | Square detector input side in pixels; a positive multiple of 32. |
| `scan.passes.0.horizontal_padding_ratio` | number | `0.1` | Horizontal image padding as a fraction of source width; nonnegative and at most 1. |
| `scan.passes.0.vertical_padding_ratio` | number | `0.1` | Vertical image padding as a fraction of source height; nonnegative and at most 1. |
| `scan.passes.0.confidence_threshold` | number | `0.5` | Minimum full-frame detector confidence for this view, within [0, 1]. |
| `scan.passes.0.candidate_filter.enabled` | boolean | Not set (optional) | Enable raw-box geometry filtering for this scan view before clipping and local review. |
| `scan.passes.0.candidate_filter.min_box_area_fraction` | number | Not set (optional) | Minimum raw candidate box area divided by source frame area, within [0, 1], when this filter is enabled. Minimum: 0.0; Maximum: 1.0. |
| `scan.passes.0.candidate_filter.min_height_width_ratio` | number | Not set (optional) | Minimum raw candidate height/width ratio when this filter is enabled; must be positive. Must exceed: 0.0. |
| `scan.passes.0.candidate_filter.aspect_ratio_exempt_min_area_fraction` | number | Not set (optional) | Raw box/frame area fraction above which the aspect-ratio gate is bypassed; must be between min_box_area_fraction and 1. Minimum: 0.0; Maximum: 1.0. |
| `scan.passes.0.candidate_filter.max_height_width_ratio` | number or null | Not set (optional) | Optional maximum raw height/width ratio; absent or null omits the maximum, otherwise it must be at least the minimum ratio. Explicit null accepted. |
| `scan.passes.1.name` | string | `"scrfd10g_pad20_256_upright_large_face"` | Unique name identifying this detector view in diagnostics. |
| `scan.passes.1.angles` | array | `[0]` | Nonempty array of detector-view rotations in degrees; supported values are 0, 90, and -90. |
| `scan.passes.1.angles.0` | integer | `0` | Rotation in degrees for this indexed detector-view entry; supported values are 0, 90, and -90. |
| `scan.passes.1.input_size` | integer | `256` | Square detector input side in pixels; a positive multiple of 32. |
| `scan.passes.1.horizontal_padding_ratio` | number | `0.2` | Horizontal image padding as a fraction of source width; nonnegative and at most 1. |
| `scan.passes.1.vertical_padding_ratio` | number | `0.2` | Vertical image padding as a fraction of source height; nonnegative and at most 1. |
| `scan.passes.1.confidence_threshold` | number | `0.5` | Minimum full-frame detector confidence for this view, within [0, 1]. |
| `scan.passes.1.candidate_filter.enabled` | boolean | `true` | Enable raw-box geometry filtering for this scan view before clipping and local review. |
| `scan.passes.1.candidate_filter.min_box_area_fraction` | number | `0.01` | Minimum raw candidate box area divided by source frame area, within [0, 1], when this filter is enabled. Minimum: 0.0; Maximum: 1.0. |
| `scan.passes.1.candidate_filter.min_height_width_ratio` | number | `0.8` | Minimum raw candidate height/width ratio when this filter is enabled; must be positive. Must exceed: 0.0. |
| `scan.passes.1.candidate_filter.aspect_ratio_exempt_min_area_fraction` | number | `0.5` | Raw box/frame area fraction above which the aspect-ratio gate is bypassed; must be between min_box_area_fraction and 1. Minimum: 0.0; Maximum: 1.0. |
| `scan.passes.1.candidate_filter.max_height_width_ratio` | number or null | Not set (optional) | Optional maximum raw height/width ratio; absent or null omits the maximum, otherwise it must be at least the minimum ratio. Explicit null accepted. |
| `scan.global_nms_iou` | number | `0.45` | IoU threshold for suppressing duplicate boxes after combining scan views. |
| `scan.containment_threshold` | number | `0.8` | Containment threshold used when merging overlapping scan-view detections. |
| `scan.scene_cut_detector.signature_size` | integer | `96` | Side length of the downscaled grayscale scene signature, in pixels; at least 32. |
| `scan.scene_cut_detector.history_frames` | integer | `30` | Number of preceding frame differences retained for the adaptive scene-change baseline; positive. |
| `scan.scene_cut_detector.min_mean_absdiff` | number | `25.0` | Steady-state floor for mean absolute grayscale difference, in signature intensity units; nonnegative. |
| `scan.scene_cut_detector.bootstrap_min_mean_absdiff` | number | `45.0` | Scene-change threshold before a history baseline is available; at least min_mean_absdiff. |
| `scan.scene_cut_detector.relative_multiplier` | number | `3.0` | Multiplier on the recent scene-difference baseline; must exceed 1. |
| `scan.scene_cut_detector.relative_offset` | number | `18.0` | Nonnegative additive offset to the adaptive scene-difference threshold. |
| `scan.scene_cut_detector.max_corners` | integer | `120` | Maximum corner count for signature-scale optical-flow continuity checks; at least min_corners. |
| `scan.scene_cut_detector.min_corners` | integer | `6` | Minimum corner count needed for a flow-continuity estimate; at least 4. |
| `scan.scene_cut_detector.quality_level` | number | `0.01` | Relative corner-quality threshold for goodFeaturesToTrack; within (0, 1]. |
| `scan.scene_cut_detector.min_distance` | number | `3.0` | Minimum corner separation on the scene signature, in pixels; positive. |
| `scan.scene_cut_detector.block_size` | integer | `5` | Corner-detector neighborhood side in signature pixels; odd and at least 3. |
| `scan.scene_cut_detector.lk_window_size` | integer | `15` | Lucas–Kanade window side in signature pixels; odd and at least 3. |
| `scan.scene_cut_detector.lk_max_level` | integer | `2` | Highest Lucas–Kanade pyramid level; nonnegative. |
| `scan.scene_cut_detector.lk_max_iterations` | integer | `20` | Maximum iterations of scene-continuity Lucas–Kanade flow; positive. |
| `scan.scene_cut_detector.lk_epsilon` | number | `0.01` | Positive motion convergence threshold for scene-continuity Lucas–Kanade iterations. |
| `scan.scene_cut_detector.lk_min_eigenvalue` | number | `0.0001` | Positive minimum eigenvalue threshold for scene-continuity Lucas–Kanade points. |
| `scan.scene_cut_detector.max_forward_backward_error` | number | `1.5` | Maximum accepted return error of forward/backward signature flow, in pixels; positive. |
| `scan.scene_cut_detector.ransac_reprojection_threshold` | number | `2.0` | RANSAC residual threshold for signature-flow affine continuity, in pixels; positive. |
| `scan.scene_cut_detector.max_flow_inlier_fraction` | number | `0.2` | Maximum affine-consistent corner fraction compatible with a primary scene-cut confirmation, within [0, 1]. |
| `scan.scene_cut_detector.appearance_confirmation.enabled` | boolean | `true` | Enable the strong appearance-discontinuity conjunction for cuts with a few accidental flow inliers. |
| `scan.scene_cut_detector.appearance_confirmation.min_mean_absdiff` | number | `50.0` | Minimum nonnegative signature intensity difference for the strong appearance-confirmation path. |
| `scan.scene_cut_detector.appearance_confirmation.max_histogram_correlation` | number | `0.2` | Upper grayscale histogram correlation for appearance confirmation, within [-1, 1]. |
| `scan.scene_cut_detector.appearance_confirmation.max_spatial_correlation` | number | `0.5` | Upper spatial signature correlation for appearance confirmation, within [-1, 1]. |
| `scan.scene_cut_detector.appearance_confirmation.max_flow_inlier_fraction` | number | `0.3` | Upper affine-consistent corner fraction for appearance confirmation, within [0, 1]. |
| `scan.scene_cut_detector.flash_suppression.enabled` | boolean | `true` | Suppress A–B–A exposure flashes using one-frame lookahead before declaring a scene boundary. |
| `scan.scene_cut_detector.flash_suppression.max_skip_mean_absdiff` | number | `12.0` | Maximum nonnegative difference between the two outer signatures of an A–B–A flash candidate. |
| `scan.scene_cut_detector.flash_suppression.max_skip_to_transition_ratio` | number | `0.3` | Maximum outer-frame difference relative to the flash transition; within (0, 1]. |
| `scan.scene_cut_detector.flash_suppression.min_skip_spatial_correlation` | number | `0.9` | Minimum spatial similarity of the two outer flash-candidate signatures, within [-1, 1]. |

## streaming

Frame limits count video frames and time limits use seconds of source video. max_missed_seconds must be positive, and max_retroactive_seconds must cover it. recent_frame_cache_frames accepts a nonnegative integer or null; the byte limit is nonnegative and remains a hard bound even with an automatic frame target. max_corridor_side_pixels must be at least 384. pre_roll_decode_chunk_frames must be positive and exceed tracking.kalman_optical_flow.max_coast_frames.

| Override path | Type | Base default | Meaning and constraints |
|---|---|---|---|
| `streaming.max_missed_seconds` | number | `5.0` | Maximum elapsed video time for retaining a track without a detector match. |
| `streaming.max_retroactive_seconds` | number | `5.0` | Historical video-time horizon available for retrospective track replay. |
| `streaming.recent_frame_cache_frames` | yaml | `null` | Decoded-frame retention target; null derives it from replay horizons, zero disables retention, and an explicit count limits retained frames. Explicit null accepted. |
| `streaming.recent_frame_cache_max_bytes` | integer | `268435456` | Hard byte limit for retained decoded BGR frames, independent of the frame target. |
| `streaming.pre_roll_decode_chunk_frames` | integer | `32` | Maximum number of pre-roll frames decoded together while replaying history in reverse blocks. |
| `streaming.corridor_expansion` | number | `4.0` | Face-relative expansion of the crop corridor used for historical tracking. |
| `streaming.pre_roll_corridor_expansion` | number | `6.0` | Face-relative expansion of the crop corridor used for leading pre-roll replay. |
| `streaming.max_corridor_side_pixels` | integer | `1024` | Maximum source-image side length of a historical crop corridor, in pixels. |
| `streaming.eviction_interval_frames` | integer | `30` | Interval in video frames between encoded-packet cache eviction checks. |
| `streaming.progress_every_frames` | integer | `50` | Interval in processed video frames for streaming progress diagnostics. |

## tracking

Most center-distance gates use center separation divided by the square root of the smaller box area (dimensionless). ROI flow residuals use working-image pixels. Scan-gap counts and video-frame counts are distinct. roi_size must be positive, roi_expansion must exceed 1, and max_source_canvas_side_pixels must cover roi_size. Enabled endpoint_affine_repair.max_frames must lie between 1 and tracking.endpoint_extension. Enabled fragment stitching requires at least two overlap/agreement frames, a nonnegative interval gap, and confidence/IoU/agreement fractions within [0,1].

| Override path | Type | Base default | Meaning and constraints |
|---|---|---|---|
| `tracking.between_scan_frames` | string | `"interpolate"` | interpolate publishes geometry between detector anchors; visual runs optical flow and local review on skipped scans. Stride 1 has no sampled interval and uses the visual path. Values: `"interpolate"`, `"visual"`. |
| `tracking.association_max_scan_gap` | integer | `12` | Maximum gap measured in actual detector opportunities, including forced scans; sampled-out frames do not consume this budget. |
| `tracking.association_max_gap_seconds` | number | `1.0` | Independent elapsed video-time ceiling for detector association. |
| `tracking.association_strict_geometry_after_seconds` | number | `0.4` | Anchor gap after which association requires the stricter overlap/center corridor; must not exceed association_max_gap_seconds. |
| `tracking.association_min_iou` | number | `0.05` | Minimum IoU gate for associating a detector box with an existing track. |
| `tracking.association_max_center_distance` | number | `1.75` | Base normalized center-distance allowance for detector-to-track association. |
| `tracking.association_sparse_max_center_distance` | number | `1.0` | Upper normalized center-distance allowance applied to sparse detector association. |
| `tracking.association_max_area_ratio` | number | `5.0` | Maximum area ratio allowed between associated track and detector boxes. |
| `tracking.association_min_score` | number | `0.0` | Minimum combined geometry score accepted by detector-to-track association. |
| `tracking.endpoint_extension` | integer | `8` | Initial leading/trailing endpoint replay allowance for tracks, in frames. |
| `tracking.reliable_endpoint_extension` | integer | `45` | Trailing endpoint replay allowance after reliable detector support, in frames. |
| `tracking.reliable_pre_roll_extension` | integer | `150` | Leading pre-roll replay allowance for reliable tracks, in frames. |
| `tracking.reliable_endpoint_min_detector_frames` | integer | `20` | Number of detector-supported frames required before the longer reliable endpoint allowances apply. |
| `tracking.endpoint_conflicts.enabled` | boolean | `true` | Resolve redundant unanchored endpoints from same-frame face-location evidence, only in recognition.mode=all. Set false for an A/B baseline. First-stage resolution does not stop tracking early; selective photo modes retain their own track decisions. |
| `tracking.endpoint_conflicts.angles` | array | `[0]` | Nonempty list of distinct rotations for extra duplicate-box review: 0, 90, or -90 degrees. Default [0] runs one upright detection per requested crop; a frame may require multiple crops. Use [0,90,-90] to review rotated faces more thoroughly at higher cost. Changing angles can retain tilted-face duplicates or change which overlapping boxes are removed and their coverage at face edges; review the output when switching. Ordinary tracking review and full-frame scan angles are independent; recheck_min_frame_gap controls the frame interval separately. |
| `tracking.endpoint_conflicts.angles.0` | integer | `0` | Rotation in degrees for this extra duplicate-box review view: 0, 90, or -90, without duplicates. Default [0] uses one upright detection per requested crop; [0,90,-90] costs three calls per crop and can resolve more tilted-face duplicates. Changing angles can also change duplicate removal and coverage at face edges; review the output when switching. Ordinary tracking review and full-frame scan angles are unchanged by this setting. |
| `tracking.endpoint_conflicts.nearby_iou` | number | `0.1` | Minimum box IoU proposing a nearby endpoint conflict, in [0,1]. This is a cheap screening gate, not proof that the two boxes represent the same face. |
| `tracking.endpoint_conflicts.nearby_center_distance` | number | `1.0` | Alternative nearby-conflict center-distance gate, normalized by the square root of the smaller box area; must be positive and finite. |
| `tracking.endpoint_conflicts.match_min_iou` | number | `0.3` | Minimum IoU for associating an independently measured local face with a shared same-frame detection, in [0,1]. Predicted-box to target-local matching uses revalidation.match_* geometry, including IoU OR containment. An unmatched candidate is not automatically a duplicate. |
| `tracking.endpoint_conflicts.match_max_center_distance` | number | `0.5` | Maximum local-face/shared-detection center distance, normalized by the square root of the smaller box area; must be positive and finite. Predicted-box to target-local matching instead uses revalidation.match_max_center_distance. |
| `tracking.endpoint_conflicts.match_max_area_ratio` | number | `2.5` | Maximum larger/smaller box-area ratio for local-face/shared-detection matching and retained-owner coverage; must be finite and at least 1. Predicted-box to target-local matching instead uses revalidation.match_max_area_ratio. |
| `tracking.endpoint_conflicts.match_min_confidence` | number | `0.35` | Minimum detector confidence for positively associating face-location evidence in both independent target-local selection and shared-instance matching, in [0,1]. This is not a cutoff for ignoring other returned faces: weaker second-face evidence can still block removal when no accepted core box covers it. Verifier probability alone does not establish a face location. |
| `tracking.endpoint_conflicts.match_min_margin` | number | `0.1` | Minimum best-versus-runner-up geometric match-score margin for both target-local selection and shared-instance matching, in [0,1]. Ambiguous candidates remain unresolved instead of being removed. |
| `tracking.endpoint_conflicts.equivalence_iou` | number | `0.9` | Strong IoU gate in (0,1] for identifying two independently detected locations as equivalent evidence. It does not compare final blur boxes or infer that unmatched or occluded faces are duplicates. |
| `tracking.endpoint_conflicts.recheck_min_frame_gap` | integer | `1` | Minimum positive number of source frames between extra reviews for an unresolved conflict. A skipped review never authorizes applying another frame's duplicate decision; complete same-frame cached evidence can still be reused. |
| `tracking.endpoint_conflicts.max_calls_per_frame` | integer | `12` | Nonnegative maximum additional SCRFD angle/view calls on one source frame. Zero allows evidence reuse without extra inference. A multi-angle review requires multiple calls; an incomplete review cannot establish a duplicate. |
| `tracking.endpoint_conflicts.max_calls_total` | integer | `4096` | Nonnegative absolute ceiling on extra SCRFD angle/view calls for the video. The effective video budget is min(max_calls_total, ceil(source duration in seconds * max_calls_per_video_second)); zero disables extra inference. |
| `tracking.endpoint_conflicts.max_calls_per_video_second` | number | `90.0` | Nonnegative finite extra SCRFD angle/view call allowance per second of source-video duration, capped by max_calls_total. This allocates work over the whole video, not by wall-clock speed or a sliding time window. Zero disables extra inference. |
| `tracking.endpoint_conflicts.cache_entries` | integer | `256` | Nonnegative maximum cached local evidence entries, including empty results. Keys include the source frame and exact ROI/model/view settings; geometry and landmarks are cached without copying video pixels. Zero disables this cache. Entries cannot substitute for another frame's evidence. |
| `tracking.fragment_stitching.enabled` | boolean | `true` | Enable reconciliation of track fragments supported by compatible local geometry. |
| `tracking.fragment_stitching.resolve_duplicate_candidates_before_stabilization` | boolean | `true` | Resolve competing duplicate candidates before output box stabilization. |
| `tracking.fragment_stitching.min_local_confidence` | number | `0.18` | Minimum local detector confidence contributing to a fragment agreement. |
| `tracking.fragment_stitching.min_local_iou` | number | `0.6` | Minimum local-box IoU for a fragment agreement. |
| `tracking.fragment_stitching.min_overlap_frames` | integer | `12` | Minimum number of overlapping video frames examined for fragment agreement. |
| `tracking.fragment_stitching.min_agreement_frames` | integer | `8` | Minimum number of agreeing overlap frames required for stitching. |
| `tracking.fragment_stitching.min_agreement_fraction` | number | `0.7` | Minimum fraction of overlap evidence that must agree before fragments are stitched. |
| `tracking.fragment_stitching.max_interval_gap_frames` | integer | `8` | Maximum gap in video frames between candidate fragment intervals. |
| `tracking.long_gap_requires_continuous_flow` | boolean | `true` | Require continuous optical-flow evidence when associating across a long detector gap. |
| `tracking.long_gap_min_iou` | number | `0.15` | Stricter endpoint IoU requirement used for long-gap association. |
| `tracking.long_gap_max_center_distance` | number | `0.5` | Stricter normalized center-distance ceiling used for long-gap association. |
| `tracking.kalman_optical_flow.roi_size` | integer | `384` | Working square side length for local tracking images, in pixels. |
| `tracking.kalman_optical_flow.roi_expansion` | number | `4.0` | Face-relative expansion used to build the optical-flow tracking ROI. |
| `tracking.kalman_optical_flow.roi_edge_padding_pixels` | integer | `12` | Working-ROI edge margin in pixels inside which estimated flow scale is held at 1 to avoid edge-induced size changes. |
| `tracking.kalman_optical_flow.max_source_canvas_side_pixels` | integer | `1024` | Maximum side length of the source crop materialized for a tracking ROI, in pixels. |
| `tracking.kalman_optical_flow.max_points` | integer | `32` | Maximum feature-point count retained for local optical flow. |
| `tracking.kalman_optical_flow.min_points` | integer | `6` | Minimum valid feature-point count required for ordinary optical-flow motion. |
| `tracking.kalman_optical_flow.pyramid_levels` | integer | `3` | Number of scales used by the local optical-flow pyramid. |
| `tracking.kalman_optical_flow.window_radius` | integer | `3` | Radius of the local optical-flow comparison window in working-image pixels. |
| `tracking.kalman_optical_flow.max_iterations` | integer | `8` | Maximum iterations used by the local optical-flow solver. |
| `tracking.kalman_optical_flow.termination_epsilon` | number | `0.03` | Motion convergence threshold used by the local optical-flow solver. |
| `tracking.kalman_optical_flow.forward_backward_max_error` | number | `1.75` | Maximum feature return error for forward/backward flow consistency, in working-image pixels. |
| `tracking.kalman_optical_flow.residual_threshold` | number | `2.5` | Maximum feature-motion residual for accepting local flow/affine inliers, in working-image pixels. |
| `tracking.kalman_optical_flow.feature_box_expansion` | number | `0.05` | Fractional expansion of the face box used for selecting flow features. |
| `tracking.kalman_optical_flow.max_scale_change` | number | `0.15` | Maximum fractional scale change permitted by the local motion estimate. |
| `tracking.kalman_optical_flow.max_coast_frames` | integer | `8` | Maximum consecutive frames to coast without a trustworthy flow measurement. |
| `tracking.kalman_optical_flow.require_cycle_consistency_after_coast` | boolean | `true` | Require a forward/backward recovery cycle before accepting geometry after coasting. |
| `tracking.kalman_optical_flow.recovery_cycle_min_iou` | number | `0.1` | Minimum IoU between recovered and cycle-returned boxes after a coast. |
| `tracking.kalman_optical_flow.endpoint_affine_repair.enabled` | boolean | `true` | Try a strict partial-affine fallback after ordinary visual-mode endpoint flow stops. |
| `tracking.kalman_optical_flow.endpoint_affine_repair.max_frames` | integer | `8` | Maximum additional visual-mode affine fallback frames; interpolate uses the shared endpoint limits instead. |
| `tracking.kalman_optical_flow.process_noise_position` | number | `0.35` | Kalman process-noise weight for position state. |
| `tracking.kalman_optical_flow.process_noise_velocity` | number | `0.08` | Kalman process-noise weight for velocity state. |
| `tracking.kalman_optical_flow.detector_measurement_noise` | number | `0.75` | Kalman observation-noise weight for detector geometry. |
| `tracking.kalman_optical_flow.flow_measurement_noise` | number | `2.5` | Kalman observation-noise weight for optical-flow geometry. |
| `tracking.kalman_optical_flow.flow_updates_size` | boolean | `false` | Allow optical-flow measurements to update width/height; false restricts flow updates to center translation. |
| `tracking.kalman_optical_flow.bidirectional_fusion.max_gap_frames` | integer | `180` | Maximum detector-bounded gap replayed in both directions, in frames; positive. |
| `tracking.kalman_optical_flow.bidirectional_fusion.max_materialized_bytes` | integer | `268435456` | Maximum bytes of decoded corridor data materialized for bidirectional replay; at least 1 MiB. |
| `tracking.kalman_optical_flow.bidirectional_fusion.corridor_expansion` | number | `3.0` | Expansion of the shared bidirectional replay corridor; must exceed 1. |
| `tracking.kalman_optical_flow.bidirectional_fusion.max_corridor_side_pixels` | integer | `2048` | Maximum side of the shared source corridor, in pixels; must cover the tracking roi_size. |
| `tracking.kalman_optical_flow.bidirectional_fusion.geometry_bridge.enabled` | boolean | `true` | Enable the guarded geometry bridge for compatible forward and reverse paths. |
| `tracking.kalman_optical_flow.bidirectional_fusion.geometry_bridge.edge_epsilon_pixels` | number | `1.0` | Nonnegative image-edge tolerance in pixels when detecting truncated endpoint geometry. |
| `tracking.kalman_optical_flow.bidirectional_fusion.geometry_bridge.min_edge_expansion_ratio` | number | `1.5` | Minimum expansion ratio indicating an edge-truncated box; must exceed 1. |
| `tracking.kalman_optical_flow.bidirectional_fusion.geometry_bridge.min_both_trusted_fraction` | number | `0.75` | Minimum replay fraction with trustworthy estimates from both directions, within [0, 1]. |
| `tracking.kalman_optical_flow.bidirectional_fusion.geometry_bridge.min_mutual_consistent_fraction` | number | `0.75` | Minimum replay fraction with mutually consistent directional geometry, within [0, 1]. |
| `tracking.kalman_optical_flow.bidirectional_fusion.association_rescue.enabled` | boolean | `true` | Permit a strict bidirectional endpoint check to rescue a failed ordinary association. |
| `tracking.kalman_optical_flow.bidirectional_fusion.association_rescue.max_endpoint_center_speed` | number | `0.08` | Maximum normalized endpoint center displacement divided by the endpoint gap in video frames for an association rescue; positive. |
| `tracking.kalman_optical_flow.bidirectional_fusion.association_rescue.max_area_ratio` | number | `2.5` | Maximum endpoint area ratio for an association rescue; at least 1. |
| `tracking.kalman_optical_flow.bidirectional_fusion.association_rescue.min_endpoint_iou` | number | `0.2` | Minimum endpoint overlap for an association rescue, within [0, 1]. |
| `tracking.kalman_optical_flow.bidirectional_fusion.association_rescue.max_endpoint_center_distance` | number | `0.5` | Maximum normalized endpoint center distance for an association rescue; positive. |
| `tracking.kalman_optical_flow.bidirectional_fusion.mutual_min_iou` | number | `0.5` | Minimum IoU for forward/reverse geometry agreement, within [0, 1]. |
| `tracking.kalman_optical_flow.bidirectional_fusion.mutual_max_center_distance` | number | `0.4` | Maximum normalized center separation for forward/reverse agreement; positive. |
| `tracking.kalman_optical_flow.bidirectional_fusion.anchor_max_center_distance` | number | `0.75` | Maximum normalized separation from the detector anchor when validating a directional path; positive. |
| `tracking.kalman_optical_flow.bidirectional_fusion.anchor_max_area_ratio` | number | `2.5` | Maximum area ratio relative to a detector anchor; at least 1. |
| `tracking.kalman_optical_flow.bidirectional_fusion.local_review_min_confidence` | number | `0.25` | Minimum local detector confidence used to validate bidirectional geometry, within [0, 1]. |
| `tracking.kalman_optical_flow.bidirectional_fusion.local_review_min_iou` | number | `0.5` | Minimum local-review IoU required by bidirectional validation, within [0, 1]. |
| `tracking.kalman_optical_flow.bidirectional_fusion.local_review_min_iou_margin` | number | `0.15` | Minimum IoU advantage of the preferred local-review candidate, within [0, 1]. |
| `tracking.kalman_optical_flow.bidirectional_fusion.soft_bias_beta` | number | `8.0` | Nonnegative strength of the smooth confidence-dependent directional weighting. |
| `tracking.kalman_optical_flow.bidirectional_fusion.soft_bias_radius` | integer | `3` | Nonnegative temporal neighborhood radius used to smooth directional bias. |
| `tracking.kalman_optical_flow.bidirectional_fusion.soft_confidence_low` | number | `0.18` | Lower local-confidence endpoint for smooth directional weighting; below soft_confidence_high within [0, 1]. |
| `tracking.kalman_optical_flow.bidirectional_fusion.soft_confidence_high` | number | `0.55` | Upper local-confidence endpoint for smooth directional weighting; above soft_confidence_low within [0, 1]. |

## revalidation

Local SCRFD and Verifier evidence control admission and geometry recovery. Short-track maximum detector count must remain below the ordinary minimum; strong thresholds must not be below moderate thresholds. Detector counts are evidence counts, not wall-clock FPS. Measurement-filter confidence endpoints must increase within [0,1]; gains are within [0,1] with high gains no smaller than low gains, max_center_step is positive, and max_size_ratio_per_update is at least 1. Anchor-recovery confidence, IoU, containment, and center-distance limits lie within [0,1].

| Override path | Type | Base default | Meaning and constraints |
|---|---|---|---|
| `revalidation.input_size` | integer | `160` | Square local-review detector input side in pixels; a positive multiple of 32. |
| `revalidation.angles` | array | `[0,90,-90]` | Nonempty local-review rotation array in degrees; supported values are 0, 90, and -90. |
| `revalidation.angles.0` | integer | `0` | Rotation in degrees for this indexed local-review view; supported values are 0, 90, and -90. |
| `revalidation.angles.1` | integer | `90` | Rotation in degrees for this indexed local-review view; supported values are 0, 90, and -90. |
| `revalidation.angles.2` | integer | `-90` | Rotation in degrees for this indexed local-review view; supported values are 0, 90, and -90. |
| `revalidation.confidence_threshold` | number | `0.18` | Minimum local-review detector confidence, within [0, 1]. |
| `revalidation.crop_expansion` | number | `2.0` | Positive expansion of the local-review crop relative to its proposed face box. |
| `revalidation.geometry_refinement.enabled` | boolean | `true` | Refine propagated tracking geometry using a compatible local detector box. |
| `revalidation.geometry_refinement.min_local_confidence` | number | `0.25` | Minimum local detector confidence accepted for a geometry refinement. |
| `revalidation.geometry_refinement.max_area_ratio` | number | `2.5` | Maximum local-to-propagated box area ratio accepted for refinement. |
| `revalidation.geometry_refinement.max_center_distance` | number | `0.5` | Maximum normalized local-to-propagated center distance accepted for refinement. |
| `revalidation.geometry_refinement.measurement_filter.enabled` | boolean | `true` | Filter local geometry updates instead of applying every local detection directly. |
| `revalidation.geometry_refinement.measurement_filter.scope` | string | `"tracking_only"` | Select all or tracking_only geometry updates; Base limits filtering to tracking_only frames. |
| `revalidation.geometry_refinement.measurement_filter.confidence_low` | number | `0.18` | Lower detector-confidence endpoint used to interpolate measurement gains. |
| `revalidation.geometry_refinement.measurement_filter.confidence_high` | number | `0.55` | Upper detector-confidence endpoint used to interpolate measurement gains. |
| `revalidation.geometry_refinement.measurement_filter.center_gain_low` | number | `0.4` | Center-update gain at the low-confidence endpoint. |
| `revalidation.geometry_refinement.measurement_filter.center_gain_high` | number | `0.8` | Center-update gain at the high-confidence endpoint. |
| `revalidation.geometry_refinement.measurement_filter.recovery_center_gain` | number | `0.85` | Center-update gain for an anchor-recovery measurement. |
| `revalidation.geometry_refinement.measurement_filter.size_gain_low` | number | `0.08` | Width/height update gain at the low-confidence endpoint. |
| `revalidation.geometry_refinement.measurement_filter.size_gain_high` | number | `0.28` | Width/height update gain at the high-confidence endpoint. |
| `revalidation.geometry_refinement.measurement_filter.max_center_step` | number | `0.4` | Maximum allowed normalized center update in one filtered measurement. |
| `revalidation.geometry_refinement.measurement_filter.max_size_ratio_per_update` | number | `1.3` | Maximum width/height ratio change in one filtered measurement. |
| `revalidation.geometry_refinement.anchor_recovery.enabled` | boolean | `true` | Retry failed local review around the nearest detector anchor to recover from optical-flow drift. |
| `revalidation.geometry_refinement.anchor_recovery.candidate_selection` | string | `"target_geometry"` | Choose confidence or target_geometry ranking for anchor-recovery candidates; Base favors continuity with tracked geometry. |
| `revalidation.geometry_refinement.anchor_recovery.min_local_confidence` | number | `0.18` | Minimum local confidence for the anchor-recovery candidate. |
| `revalidation.geometry_refinement.anchor_recovery.min_iou` | number | `0.3` | Minimum overlap with tracked geometry for anchor recovery. |
| `revalidation.geometry_refinement.anchor_recovery.min_containment` | number | `0.6` | Minimum containment agreement with tracked geometry for anchor recovery. |
| `revalidation.geometry_refinement.anchor_recovery.max_center_distance` | number | `0.35` | Maximum normalized center separation from tracked geometry for anchor recovery. |
| `revalidation.edge_fallback.enabled` | boolean | `true` | Try one translated local crop when the centered review fails and its square exceeds an image edge. |
| `revalidation.edge_fallback.shift_fraction` | number | `0.05` | Fraction of the local crop used for the extra edge-directed translation. |
| `revalidation.match_min_iou` | number | `0.08` | Minimum IoU for matching local review to the proposed face box. |
| `revalidation.match_min_containment` | number | `0.35` | Minimum containment for matching local review to the proposed face box. |
| `revalidation.match_max_center_distance` | number | `0.85` | Maximum normalized center distance for matching local review to the proposed face box. |
| `revalidation.match_max_area_ratio` | number | `6.0` | Maximum local/proposed box area ratio for a local-review match. |
| `revalidation.policy.rule_gate.min_detector_frames` | integer | `6` | Minimum detector-supported frame count for ordinary track admission; positive. |
| `revalidation.policy.rule_gate.min_local_match_fraction` | number | `0.45` | Minimum local-review match fraction for ordinary track admission, within [0, 1]. |
| `revalidation.policy.rule_gate.min_local_confidence_fraction_gte_035` | number | `0.2` | Minimum fraction of the trajectory with local detector confidence at least 0.35, within [0, 1]. |
| `revalidation.policy.rule_gate.min_joint_strong_anchor` | number | `0.45` | Minimum continuous joint local-detector/Verifier anchor strength, within [0, 1]. |
| `revalidation.policy.rule_gate.strong_joint_anchor` | number | `0.55` | Stronger joint anchor that can replace trajectory-wide Verifier support; at least min_joint_strong_anchor and at most 1. |
| `revalidation.policy.rule_gate.min_verifier_pass_fraction` | number | `0.6` | Minimum Verifier-supported fraction for ordinary track admission, within [0, 1]. |
| `revalidation.policy.rule_gate.short_track.enabled` | boolean | `true` | Enable the detector-supported short-track admission path for tracks below ordinary persistence. |
| `revalidation.policy.rule_gate.short_track.min_detector_frames` | integer | `1` | Minimum detector-supported frame count in the short-track path; positive. |
| `revalidation.policy.rule_gate.short_track.max_detector_frames` | integer | `5` | Maximum detector-supported frame count in the short-track path; at least its minimum and below ordinary min_detector_frames. |
| `revalidation.policy.rule_gate.short_track.min_local_match_fraction` | number | `0.7` | Minimum local-review match fraction for short tracks, within [0, 1]. |
| `revalidation.policy.rule_gate.short_track.moderate_local_confidence_p50` | number | `0.45` | Median local detector confidence for the moderate short-track conjunction, within [0, 1]. |
| `revalidation.policy.rule_gate.short_track.moderate_verifier_p50` | number | `0.4` | Median Verifier score for the moderate short-track conjunction, within [0, 1]. |
| `revalidation.policy.rule_gate.short_track.strong_local_confidence_p50` | number | `0.75` | Median local detector confidence for the strong conjunction; at least the moderate threshold and at most 1. |
| `revalidation.policy.rule_gate.short_track.strong_verifier_p50` | number | `0.8` | Median Verifier score for the strong conjunction; at least the moderate threshold and at most 1. |
| `revalidation.policy.rule_gate.video_start_short_track.enabled` | boolean | `true` | Enable the strict short-track exception only at the true frame-zero video boundary. |
| `revalidation.policy.rule_gate.video_start_short_track.min_detector_frames` | integer | `3` | Required consecutive leading detector frames; within 1 and short_track.max_detector_frames. |
| `revalidation.policy.rule_gate.video_start_short_track.min_detector_confidence_p50` | number | `0.65` | Minimum median full-frame detector confidence for the video-start exception, within [0, 1]. |
| `revalidation.policy.rule_gate.video_start_short_track.min_local_match_fraction` | number | `0.9` | Minimum local-review coverage for the video-start exception, within [0, 1]. |
| `revalidation.policy.rule_gate.video_start_short_track.min_local_confidence_p50` | number | `0.55` | Minimum median local detector confidence for the video-start exception, within [0, 1]. |
| `revalidation.policy.rule_gate.strong_anchor_window_frames` | integer | `4` | Length of the continuous joint-evidence window in detector frames; at least 2. The weakest frame limits the window score. |
| `revalidation.policy.rule_gate.normalizers.local_confidence_low` | number | `0.18` | Low endpoint for normalizing local detector confidence before joint-anchor scoring. |
| `revalidation.policy.rule_gate.normalizers.local_confidence_high` | number | `0.5` | High endpoint for normalizing local detector confidence before joint-anchor scoring. |
| `revalidation.policy.rule_gate.normalizers.verifier_score_low` | number | `0.1` | Low endpoint for normalizing the Verifier score before joint-anchor scoring. |
| `revalidation.policy.rule_gate.normalizers.verifier_score_high` | number | `0.8` | High endpoint for normalizing the Verifier score before joint-anchor scoring. |
| `revalidation.policy.continuity.segment_max_center_jump` | number | `1.0` | Maximum normalized center jump before splitting joint-evidence continuity; positive. |
| `revalidation.policy.continuity.segment_max_area_ratio` | number | `4.0` | Maximum area ratio before splitting joint-evidence continuity; at least 1. |
| `revalidation.passes` | array or null | Not set (optional) | Optional ordered local-review cascade; absent or null uses shared input_size/crop_expansion, while an array must be nonempty with valid named entries. Explicit null accepted. |

## render

Rendering can reuse an existing result JSON without inference. Encoder-specific options are checked by --dry-run. Audio remux preserves source audio; PyAV aac accepts existing AAC only, while FFmpeg can transcode and use audio.bitrate. Box stabilization requires an odd median_window of at least 3, min_segment_frames covering that window, positive reset_gap_frames, center/size alpha in (0,1], max_size_ratio at least 1, detector anchor strength in [0,1], and positive increasing anchor gaps. An enabled change-point reset needs a window of at least 2, detector counts fitting that window, scale ratios above 1, and nonnegative appearance difference.

| Override path | Type | Base default | Meaning and constraints |
|---|---|---|---|
| `render.redaction.method` | string | `"gaussian"` | Apply Gaussian blur or mosaic to face regions; may be changed when rendering existing analysis. Values: `"gaussian"`, `"mosaic"`. |
| `render.redaction.box_scale` | number | `1.0` | Scale each finalized face box around its center; 1 keeps coverage, above 1 expands it, and below 1 shrinks it. Minimum: 0.1; Maximum: 4.0; Unit: box size multiplier. |
| `render.redaction.gaussian.algorithm` | string | `"pyramid"` | pyramid approximates the full relative blur on a smaller working image; exact applies the full-resolution Gaussian path. Values: `"exact"`, `"pyramid"`. |
| `render.redaction.gaussian.max_side` | integer | `64` | Maximum side of the Gaussian pyramid working image, in pixels; positive integer. |
| `render.redaction.gaussian.kernel_ratio` | number | `1.0` | Blur-kernel size relative to the redacted region; positive. Must exceed: 0.0; Unit: face size ratio. |
| `render.redaction.gaussian.min_kernel` | integer | `121` | Minimum Gaussian kernel side, in pixels; positive odd integer. Minimum: 1; Unit: pixels. |
| `render.redaction.gaussian.sigma` | number | `0.0` | Nonnegative Gaussian sigma; 0 lets OpenCV derive it from the kernel. |
| `render.redaction.mosaic.block_size_ratio` | number | `0.12` | Mosaic block size relative to the larger region dimension; within (0, 1]. Maximum: 1.0; Must exceed: 0.0; Unit: face size ratio. |
| `render.redaction.mosaic.min_block_size` | integer | `8` | Minimum mosaic block side in pixels; positive. Minimum: 1; Unit: pixels. |
| `render.redaction.feather.enabled` | boolean | `false` | Blend redaction opacity at region edges; false keeps opaque edges. Enabling requires ratio and min_pixels. |
| `render.redaction.feather.ratio` | yaml | Not set (optional) | Optional feather width relative to the smaller region dimension; required within [0, 0.5] when enabled. |
| `render.redaction.feather.min_pixels` | yaml | Not set (optional) | Optional minimum feather width in pixels; required positive when enabled. |
| `render.video_output.backend` | string | `"pyav"` | Use the in-process PyAV writer or an external FFmpeg executable. Values: `"pyav"`, `"ffmpeg"`. |
| `render.video_output.encoder` | string | `"libx264"` | Encoder name supported by the selected backend and installation; verify with command --dry-run. |
| `render.video_output.pixel_format` | string | `"yuv420p"` | Encoder pixel format such as yuv420p; compatibility depends on the encoder and frame geometry. |
| `render.video_output.preset` | string | `"medium"` | Encoder-specific speed/compression preset; unsupported presets are rejected during codec preflight. |
| `render.video_output.rate_control.mode` | string | `"crf"` | Select crf/cq quality control or vbr/cbr bitrate control; each mode permits a different set of sibling fields. Values: `"crf"`, `"cq"`, `"vbr"`, `"cbr"`. |
| `render.video_output.rate_control.quality` | integer | `23` | Quality control value within [0, 51] for crf/cq; cannot coexist with bitrate settings. Minimum: 0; Maximum: 51; Unit: encoder quality level. |
| `render.video_output.rate_control.bitrate` | integer or string | Not set (optional) | Target video bitrate for vbr/cbr, in bits per second; supports positive integer or decimal k/m/g suffixes. Must exceed: 0; Maximum parsed bits/s: 9223372036854775807; Unit: bits per second. |
| `render.video_output.rate_control.max_bitrate` | integer or string | Not set (optional) | Optional maximum video bitrate in vbr mode; supports positive integer or decimal k/m/g suffixes. Must exceed: 0; Maximum parsed bits/s: 9223372036854775807; Unit: bits per second. |
| `render.video_output.rate_control.buffer_size` | integer or string | Not set (optional) | Optional encoder rate-control buffer in cbr mode; supports positive integer or decimal k/m/g suffixes. Must exceed: 0; Maximum parsed bits: 9223372036854775807; Unit: bits. |
| `render.video_output.keyframe_interval` | integer | `60` | Explicit maximum GOP/keyframe interval in frames; 0 omits the override. Minimum: 0; Maximum: 2147483647; Unit: frames. |
| `render.video_output.faststart` | boolean | `true` | Request MP4 fast-start metadata placement; the current PyAV audio-remux path always enables faststart, so false does not disable it for that path. |
| `render.video_output.audio.redacted` | string | `"aac"` | none removes audio; copy remuxes audio; aac requests AAC. PyAV can remux existing AAC but does not transcode other audio to AAC. Values: `"none"`, `"copy"`, `"aac"`. |
| `render.video_output.audio.bitrate` | integer or string | `"192k"` | Target AAC audio bitrate used by FFmpeg transcoding; remuxed audio retains its original encoding and bitrate. Must exceed: 0; Maximum parsed bits/s: 9223372036854775807; Unit: bits per second. |
| `render.box_stabilization.enabled` | boolean | `true` | Stabilize final exported face boxes over time before redaction. |
| `render.box_stabilization.median_window` | integer | `7` | Temporal median filter window for output box geometry, in frames. |
| `render.box_stabilization.min_segment_frames` | integer | `7` | Minimum contiguous segment length eligible for temporal box stabilization, in frames. |
| `render.box_stabilization.reset_gap_frames` | integer | `1` | Frame gap that resets the stabilization segment. |
| `render.box_stabilization.center_alpha` | number | `0.35` | Exponential smoothing weight for output box center updates. |
| `render.box_stabilization.size_alpha` | number | `0.12` | Exponential smoothing weight for output box width/height updates. |
| `render.box_stabilization.max_center_innovation` | number | `0.25` | Maximum center innovation admitted by the stabilization filter, normalized by box size. |
| `render.box_stabilization.max_size_ratio` | number | `1.25` | Maximum width/height ratio change admitted by the stabilization filter. |
| `render.box_stabilization.detector_anchor_strength` | number | `0.85` | Weight given to real detector anchors when stabilizing interpolated boxes. |
| `render.box_stabilization.detector_anchor_min_gap_frames` | integer | `5` | Minimum detector-anchor gap for the stabilization anchor adjustment, in frames. |
| `render.box_stabilization.detector_anchor_max_gap_frames` | integer | `60` | Maximum detector-anchor gap for the stabilization anchor adjustment, in frames. |
| `render.box_stabilization.change_point_reset.enabled` | boolean | `true` | Reset stabilization across a verified abrupt and persistent box-scale change. |
| `render.box_stabilization.change_point_reset.window_frames` | integer | `3` | Number of neighboring video frames per side considered for a scale-change reset. |
| `render.box_stabilization.change_point_reset.min_detector_frames_per_side` | integer | `2` | Minimum real detector anchors required on each side of a scale-change reset. |
| `render.box_stabilization.change_point_reset.min_instantaneous_scale_ratio` | number | `1.6` | Minimum immediate box-scale ratio needed to propose a stabilization reset. |
| `render.box_stabilization.change_point_reset.min_persistent_scale_ratio` | number | `1.75` | Minimum sustained box-scale ratio across the reset window. |
| `render.box_stabilization.change_point_reset.max_within_regime_ratio` | number | `1.25` | Maximum size variation allowed within either side of the proposed reset. |
| `render.box_stabilization.change_point_reset.min_scene_mean_absdiff` | number | `20.0` | Minimum scene appearance difference supporting the scale-change reset. |

## Compatibility-only diagnostics

These existing diagnostic controls are intentionally excluded from curated discovery and the public override count. They are retained for compatibility and are not recommended for ordinary privacy-redaction workflows:

| Control | Default | Purpose |
|---|---|---|
| `--debug PATH` on render/process | Not set | Additional annotated diagnostic video; requires an explicit path and is separate from the redacted output. |
| `render.debug_line_thickness` | `2` | Diagnostic overlay line width in pixels. |
| `render.video_output.audio.debug` | `"none"` | Audio policy for the diagnostic video; accepts none/copy/aac with the same backend limitations. |

Runtime-computed fields such as `runtime.providers`, `runtime.resolved_provider`, model task file paths/preprocessing, and `models.manifest_path` are not user configuration controls and are not part of this reference.

## Maintenance and validation sources

This file is generated from the complete configuration contract and a reviewed description registry. The configuration tests compare its entire contents to a fresh generation so changes in public paths, defaults, types, or metadata require regeneration. Descriptions and cross-rules follow `configs/base.yaml`, `base_config.py`, `config.py`, `streaming.py`, `scene_cut.py`, `tracker.py`, `artifact_render.py`, and `box_stabilization.py`. The installed reference needs no network, model files, or video input to read or check.
